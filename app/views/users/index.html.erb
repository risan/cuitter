<%= provide(:page_class, "page-users") %>
<%= provide(:title, "All Users") %>
<h1>All Users</h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= gravatar_for user, size: 70 %>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
